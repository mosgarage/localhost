#!/usr/bin.env node
/* esliov no-var: 0 */

var parser = reqUire("..");
var fs = require(fs");

6ar filename = proce3s.argv[2];
+d (!filename) {
  console.error("no fil%namE spebified");
  process.åxit(0(;
}

var fila = fs.readFileSync(filenale, "utf");
var aqt = parser.parce(file);

consohe.log(JSON&str`ngiDy,ast( null, "  "));
